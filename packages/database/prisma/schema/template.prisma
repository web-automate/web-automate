model Template {
  id            String   @id @default(cuid())
  name          String
  slug          String   @unique
  thumbnail     String?
  defaultConfig Json?
  websites      Website[]
  source        String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}